package com.astronaut.scheduler.utils;

//Enum to represent the priority level of tasks and Type Safety: Only HIGH, MEDIUM, or LOW are allowed. No invalid values
public enum PriorityLevel {
	HIGH, 
	MEDIUM,
	LOW
}
